// To find the area of rectangle based on hardcoded length, breadth and with user input
#include<stdio.h>
int main(){
/*   int length = 34;
    int breadth =23;
    int area = length *breadth;
    printf("The area of rectangle %d",area);
   */
    int length;
    scanf("%d",&length);
    int breadth;
    scanf("%d",&breadth);
    int area= length *breadth;
    printf("The area of rectangle %d",area);
    return 0;

}
